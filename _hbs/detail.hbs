Detail<br />
{{user.mother}} &amp; {{user.partner}}
<input type="file" required name="photo" id="photo" accept="image/*" capture="camera" />
{{#each photos}}
<img src="{{photo_url}}" alt="" />
{{/each}}